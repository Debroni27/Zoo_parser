- В файле customs_utils.py лежат все необходимые вспомогательные сервисы,
   получение базового объекта после get запроса на ulr,
   запись данных в .csv файл

- Есть отдельные поля на проверку данных полей при парсинге продукта
  check_fields()
  find_item_quantity()
  find_item_weight()